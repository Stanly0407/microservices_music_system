pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'microservices_music_system'

include 'song-service', 'resource-service', 'eureka-server', 'api-gateway'
