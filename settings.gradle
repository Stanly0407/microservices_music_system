pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'microservices_music_system'

include 'song-service', 'resource-service', 'resource-processor', 'eureka-server', 'api-gateway', 'e2e-tests'
