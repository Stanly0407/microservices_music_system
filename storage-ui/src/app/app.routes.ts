import { Routes } from '@angular/router';
import { StoragesPageComponent } from './features/storages/storages-page.component';

export const routes: Routes = [{ path: '', component: StoragesPageComponent }];
